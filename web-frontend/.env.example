# Frontend API base URL (Vite exposes only VITE_-prefixed vars to the client).
# Used by src/api/axios.js (baseURL) and the /doctor dev proxy in vite.config.js.
# Update this to your current ngrok / cloud domain when it changes.
VITE_API_URL=https://your-ngrok-or-domain-here
